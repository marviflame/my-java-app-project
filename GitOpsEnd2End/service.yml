apiVersion: v1
kind: Service
metadata:
  name: my-java-app
  labels:
    run: my-java-app
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    run: my-java-app
